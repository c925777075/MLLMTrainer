# 🚀 MLLM Efficient Training Framework

<div align="center">

**高性能 • 易扩展 • 专为多模态大模型设计**

[项目简介](#features) | [核心亮点](#highlight) | [快速开始](#quick-start)

</div>

---

## <a id="features"></a>📖 项目简介 (Introduction)

本项目是一个专为多模态大模型 (MLLM) 设计的高效训练框架。它旨在解决大模型训练中的资源利用率问题，并提供极高的灵活性以适配各种自定义数据和模型架构。

## <a id="highlight"></a>✨ 核心亮点 (Key Features)

### 1. ⚡ Qwen3-VL x LigerKernel 深度适配
- **极致加速**：完美支持 Qwen3-VL 模型，并集成了 **LigerKernel** 算子优化。
- **显存优化**：通过底层算子融合，显著降低显存占用，提升吞吐量。

### 2. 📦 Sequence Packing (Packing 训练)
- **榨干 GPU 资源**：支持多样本 Packing 策略，消除因长度不一造成的 Padding 浪费。
- **效率倍增**：在保持精度的前提下，大幅提升训练数据的吞吐效率，实现 GPU 算力 100% 利用。

### 3. 🧩 高度灵活的自定义支持
- **自定义 Dataset**：轻松注册私有数据集，支持 JSON/Image 等多种格式。
- **自定义模型结构**：模块化设计，允许用户快速接入新的模型架构或修改现有层级。

---

## <a id="quick-start"></a>🛠 快速开始 (Quick Start)

请按照以下步骤配置并启动您的训练任务。

首先，注册您的自定义数据集。

**步骤 1：注册数据集**
修改数据集文件：
例如：

`/mnt/cxzx/share/chenyu/MLLMTrainer/config/_base_/dataset/mllm/opensource.py`

```python
OpenSourceData = dict(
    infinity_fine=dict(
        name='infinity_fine',
        type='json',
        sampling_rate="1.0",
        meta_file="/mnt/cxzx/share/chenyu/MLLMTrainer/data/infinity_fine.json",
    ),
)
```

**步骤 2：激活数据集**

修改数据集激活文件，例如：

`/mnt/cxzx/share/chenyu/MLLMTrainer/config/_base_/dataset/config.yaml`


**步骤 3: 修改训练配置**

修改核心训练配置文件，调整超参数及保存路径。

例如：配置文件路径：

`/mnt/cxzx/share/chenyu/MLLMTrainer/config/mllm/qwen3_vl.py`


**步骤 4: 配置启动脚本**

检查并修改 Bash 启动脚本。

脚本路径：

`/mnt/cxzx/share/chenyu/MLLMTrainer/script/train_qwen3_vl.sh`

**步骤 5: 🚀 启动训练**
```python
sh script/train_qwen3_vl.sh
```

**📂 目录结构说明**

```python
MLLMTrainer/
├── config/
│   ├── _base_/
│   │   └── dataset/
│   │       ├── config.yaml       # 数据集激活配置
│   │       └── mllm/
│   │           └── opensource.py # 数据集注册文件
│   └── mllm/
│       └── qwen3_vl.py           # 模型训练参数配置
├── data/                         # 数据存放目录
├── script/
│   └── train_qwen3_vl.sh         # 启动脚本
└── ...
```

### Created with ❤️ by EverMindAI Team.